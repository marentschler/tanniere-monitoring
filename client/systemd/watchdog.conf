# Raspberry Pi hardware watchdog configuration
# The daemon keeps /dev/watchdog fed while userspace is healthy.

watchdog-device = /dev/watchdog
interval = 10
realtime = yes
priority = 1

# Basic health checks
max-load-1 = 24
min-memory = 1